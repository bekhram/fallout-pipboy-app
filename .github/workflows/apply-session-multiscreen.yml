name: Apply session multiscreen

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply session multiscreen patch
        run: python3 .github/scripts/apply_session_multiscreen.py

      - name: Build
        run: npm run build

      - name: Commit changes
        run: |
          rm -f .github/scripts/apply_session_multiscreen.py
          rm -f .github/workflows/apply-session-multiscreen.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "feat: split GM session into overview combat and log screens"
          git push origin main
